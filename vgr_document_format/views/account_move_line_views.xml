<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Añadir campo not_printable a la vista de líneas de factura -->
    <record id="view_move_line_form_inherit" model="ir.ui.view">
        <field name="name">account.move.line.form.inherit</field>
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_move_line_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='account_id']" position="after">
                <field name="not_printable"/>
            </xpath>
        </field>
    </record>

    <!-- Añadir campo not_printable a la vista tree de líneas de factura en el formulario de factura -->
    <record id="view_move_form_inherit" model="ir.ui.view">
        <field name="name">account.move.form.inherit</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='invoice_line_ids']//tree//field[@name='tax_ids']" position="after">
                <field name="not_printable" string="No Imprimir"/>
            </xpath>
        </field>
    </record>
</odoo>
